.nitro
.tanstack
.wrangler
.output
.vinxi

# Generated by `pnpm run generate:sitemap` (chained into dev + build);
# tracked-file-free so the script is the single source of truth.
public/sitemap.xml

# Copied from the installed `zxing-wasm` by `pnpm run sync:wasm` (also
# chained into dev + build). Same rule: the package is the single source
# of truth. A hand-vendored copy silently drifts out of lockstep with the
# glue JS and the scanner stops decoding — see scripts/sync-zxing-wasm.ts.
public/zxing-wasm/
__unconfig*
todos.json
.env
.env.*
!.env.example

# Playwright e2e
test-results/
playwright-report/
playwright/.cache/
