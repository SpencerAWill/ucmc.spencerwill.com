config:
  ucmc-infra:accountId: d57880630b3277f59331318b53f323b6
  ucmc-infra:zoneId: 6bfb4d1d3134e6d765aef413094cd28f
  ucmc-infra:hostname: dev.ucmc.spencerwill.com
  ucmc-infra:workerName: ucmc-web-dev
  ucmc-infra:d1DatabaseName: ucmc-web-dev
  ucmc-infra:r2BucketName: ucmc-web-dev-storage
  ucmc-infra:kvNamespaceTitle: ucmc-web-dev-kv
  ucmc-infra:webauthnRpName: UCMC (dev)
  ucmc-infra:resendFromName: UCMC
  # Resend free tier allows only one verified domain per account, so the
  # prod stack owns the Resend resources and dev borrows the sending key
  # + from-address via a StackReference. See `index.ts` for the logic.
  ucmc-infra:resendOwnerStack: prod
